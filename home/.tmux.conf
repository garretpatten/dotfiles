### Tmux — modular config under ~/.config/tmux/
#
# Symlink repo config onto this machine first:
#   ~/Projects/dotfiles/setup.sh --link-xdg-config
# Then ~/.config/tmux → …/dotfiles/config/tmux and this includes base.conf + Gruvbox theme.

source-file ~/.config/tmux/includes/base.conf
