# shellcheck shell=bash
# Sourced from ~/.zshrc — sets the active dotfiles UI theme.
#
# Values: gruvbox | everforest
# Override for a single session: DOTFILES_THEME=everforest zsh
# Persist per-machine without editing this file: export DOTFILES_THEME in ~/.local_extras
export DOTFILES_THEME="${DOTFILES_THEME:-gruvbox}"
