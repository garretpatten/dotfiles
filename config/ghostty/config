### Ghostty — core options + theme include (switch in theme-*.inc)
### Copy or symlink this tree to ~/.config/ghostty/
###
### Theme: edit the single active `config-file = theme-….inc` line below
### (or duplicate this file and swap the include).

config-file = core.inc

# Active theme — use exactly one of:
config-file = theme-gruvbox.inc
# config-file = theme-everforest.inc
