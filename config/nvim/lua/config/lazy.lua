return {
  spec = require("plugins"),
  defaults = {
    lazy = false,
    version = false,
  },
  install = {
    missing = true,
    colorscheme = { "habamax" },
  },
  checker = { enabled = false },
  ui = {
    border = "rounded",
  },
  change_detection = {
    enabled = false,
    notify = false,
  },
}
